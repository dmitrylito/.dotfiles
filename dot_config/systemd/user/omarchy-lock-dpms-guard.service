[Unit]
Description=Keep reconnected monitors blank while the session is locked
PartOf=graphical-session.target
After=graphical-session.target

[Service]
Type=simple
ExecStart=%h/.local/bin/omarchy-lock-dpms-guard
Restart=always
RestartSec=1

[Install]
WantedBy=graphical-session.target
