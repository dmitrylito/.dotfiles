#!/usr/bin/env bash
set -euo pipefail
hyprctl -i 0 keyword misc:allow_session_lock_restore 1
hyprctl -i 0 dispatch exec hyprlock
echo "restore-hyprlock: relaunched hyprlock — switch back with Ctrl-Alt-F1."
